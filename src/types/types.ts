export interface ITodo {
  id: number;
  title: string;
  complated: boolean;
}
export interface INum {
  id: number;
  type?: number;
}

export interface IApi {
  id: number;
  title: string;
  url: string;
}
